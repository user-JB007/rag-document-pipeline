# Optional OpenAI-compatible generation (leave unset for offline extractive answers)
OPENAI_BASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional override for config path
# RAG_CONFIG=config/pipeline.yaml

# API source / sink (leave empty to use config/pipeline.yaml defaults)
API_HTTP_TIMEOUT=
API_HTTP_RETRIES=
SINK_API_URL=http://127.0.0.1:8089/ingest
EXTERNAL_SINK_URL=https://jsonplaceholder.typicode.com/posts
